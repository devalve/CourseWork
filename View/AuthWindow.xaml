<Window x:Class="CourseWork.View.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CourseWork.View" 
        xmlns:viewmodel="clr-namespace:CourseWork.ViewModel" 
        d:DataContext="{d:DesignInstance Type=viewmodel:DataManageVM}"
        mc:Ignorable="d"
        Title="Auth" Height="300" Width="300" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="SingleBorderWindow" SizeToContent="Manual" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" FlowDirection="LeftToRight">
    <Window.DataContext>
        <viewmodel:DataManageVM/>
    </Window.DataContext>
    <Grid>
        <StackPanel VerticalAlignment="Center" Width="150">
            <StackPanel>
                <Label>Login</Label>
                <TextBox  Margin="0,0,0,5" />
            </StackPanel>
            <StackPanel>
                <Label>Password</Label>
                <PasswordBox Margin="0,0,0,5"></PasswordBox>
            </StackPanel>
            <Button Margin="0,30,0,0" Command="{Binding OpenMainWnd}">Enter</Button>
        </StackPanel>
    </Grid>
</Window>
